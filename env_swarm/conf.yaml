version: '3.6'
services:
  auth:
    environment:
      Max_Mem: "1000M"
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: "1000M"
        reservations:
          cpus: ".5"
          memory: "500M"
      replicas: 1
